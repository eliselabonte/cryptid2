import './profile.scss';
import Dashboard from '../dashboard/Dashboard';

export default function Profile()  {
    return  (
        <div>
            <h2>PROFILE</h2>
            <p>Contains an avatar, a name, all posts from this user, and the creatures that this user has been tracking</p>
        </div>
    )
}
